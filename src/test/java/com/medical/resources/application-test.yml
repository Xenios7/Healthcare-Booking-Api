server:
  port: 0

security:
  jwt:
    # 32-byte key (Base64). Keep the quotes to avoid parsing issues with '+' and '='
    secret: 'kztivzfn2xsCsQ0D+yfCqNIHTNvtHEcexKJSWoxcp2g='
    issuer: 'bookingapi-test'
    expiration: PT1H

spring:
  main:
    banner-mode: "off"
  jpa:
    show-sql: false

logging:
  level:
    root: ERROR
    "[org.springframework.test]": INFO
    "[org.testcontainers]": WARN
    "[com.github.dockerjava]": WARN
